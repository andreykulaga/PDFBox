The program reads JSON data and creates a PDF report. Using NewJsonConfigurationRequestModel we can configure the style of tables in the report, grouping by different columns, aggregating, and so on.
